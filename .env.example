# Names only. Never put real values in this file or anywhere else in the repo.

# SHA-256 hex of the shared staff password. The live hash lives in gate.js.
# Do not write the plaintext password here.
# PASSWORD_SHA256=

# Google Sheet file id for the live catalog (not the full URL).
# Export is a private step run by an editor. Do not commit a docs.google.com link.
# SHEET_ID=
